# Paperhour

A public reading room that turns over every hour.

Write privately at `/desk`. Mark a page public and it appears on the front.
An hourly automation writes a new dispatch into the `hours` table.
